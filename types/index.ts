export type Grade = {
  id: string;
  grade: string;
};

export type Inputs = {
  name: string;
  grade: string;
  contactNumber: string;
  address: string;
};

export type Student = {
  id: number;
  name: string;
  grade: string;
  contactNumber: string;
  address: string;
};
